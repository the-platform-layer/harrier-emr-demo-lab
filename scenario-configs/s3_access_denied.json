{
  "scenario": "s3_access_denied",
  "slice": 10,
  "job": "spark-jobs/s3_access_denied/job.py",
  "intended_deploy_mode": "cluster",
  "expected_outcome": "failed",
  "expected_root_cause_category": "S3_ACCESS_DENIED",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "uses a demo-only S3 path in the failure message",
    "does not change bucket policy or IAM by default",
    "optional real denied read must point to demo resources"
  ]
}
