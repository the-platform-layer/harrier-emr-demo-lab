{
  "scenario": "db_lock_timeout",
  "slice": 14,
  "job": "spark-jobs/db_lock_timeout/job.py",
  "intended_deploy_mode": "client",
  "expected_outcome": "failed",
  "expected_root_cause_category": "DB_LOCK_TIMEOUT",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "simulates lock timeout evidence",
    "does not hold a real database lock by default"
  ]
}
