{
  "scenario": "hdfs_full",
  "slice": 14,
  "job": "spark-jobs/hdfs_full/job.py",
  "intended_deploy_mode": "cluster",
  "expected_outcome": "failed",
  "expected_root_cause_category": "HDFS_FULL",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "simulates storage pressure in logs",
    "does not write filler files to HDFS or local disk"
  ]
}
