{
  "scenario": "executor_oom",
  "slice": 10,
  "job": "spark-jobs/executor_oom/job.py",
  "intended_deploy_mode": "cluster",
  "expected_outcome": "failed",
  "expected_root_cause_category": "EXECUTOR_OOM",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "uses demo EMR cluster only",
    "uses bounded executor memory pressure defaults",
    "does not mutate IAM or shared data"
  ]
}
