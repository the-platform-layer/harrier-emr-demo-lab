{
  "scenario": "db_partition_hotspot",
  "slice": 14,
  "job": "spark-jobs/db_partition_hotspot/job.py",
  "intended_deploy_mode": "cluster",
  "expected_outcome": "failed",
  "expected_root_cause_category": "DB_PARTITION_HOTSPOT",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "uses generated Spark data to model JDBC skew",
    "does not require a live DB by default"
  ]
}
