{
  "scenario": "shuffle_spill",
  "slice": 14,
  "job": "spark-jobs/shuffle_spill/job.py",
  "intended_deploy_mode": "cluster",
  "expected_outcome": "failed",
  "expected_root_cause_category": "SHUFFLE_SPILL",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "uses bounded generated shuffle data",
    "does not fill real disks by default"
  ]
}
