{
  "scenario": "driver_oom",
  "slice": 10,
  "job": "spark-jobs/driver_oom/job.py",
  "intended_deploy_mode": "client",
  "expected_outcome": "failed",
  "expected_root_cause_category": "DRIVER_OOM",
  "cleanup_scope": [
    "uploaded job object",
    "scenario output prefix",
    "run context"
  ],
  "safety": [
    "uses demo EMR cluster only",
    "fails through bounded driver result collection",
    "does not mutate IAM or shared data"
  ]
}
